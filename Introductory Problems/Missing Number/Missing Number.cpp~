#include<bits/stdc++.h>
using namespace std;

int main(){
  int n, val;
  vector<bool>v(n+1, true);
  cin>>n;
  while(cin>>val){
    v[val]=false;
  }
  for(int i=1; i<=n; i++){
    if(v[i]){
      cout<<i;
      return 0;
    }
  }
}
