#include <bits/stdc++.h>
using namespace std;

int main(){
  int n, x;
  cin>>n>>x;
  map<int, int>m;
  for(int i=0; i<n; ++i){
    int a;
    cin>>a;
    if(map.find(x-a)==map.end())
      map[a]=i;
    else{
      cout<<map[x-a]+1<<i+1;
      return;
    }
  }
  cout<<"IMPOSSIBLE";
}
