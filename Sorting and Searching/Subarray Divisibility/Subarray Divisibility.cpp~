#include <bits/stdc++.h>
using namespace std;

int main(){
  int n;
  cin>>n;
  int sum=0, ans=0;
  map<int, int>m;
  for(int i=0; i<n; ++i){
    int x;
    cin>>x;
    s=(s+x)%n;
    ans+=m[s];
    m[s]++;
  }
}
